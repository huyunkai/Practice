/*
 * 演示tcp传输.
 * 1,tcp分客户端和服务端.
 * 2,客户端对应的对象是Socket.
 *   服务端对应的对象是ServerSocket.
 *   需求：
 *   给服务端发送文本数据.
 *   
 
 */
/*
 *客服端,
 *通过查阅socket对象，发现在该对象建立时，就可以去连接指定主机.
 *因为tcp是面向连接的，所以在建立socket服务时,就需要有服务器存在，兵连接成功.
 *形成通路后，在该通道进行数据的传输.
 *步骤：
 *1,创建Socket服务并指定要连接的主机和端口.
 *2, 
 */
package Awt;

public class TCPDemo {
	public static void main(String []args)
	{
		
	}
}
